# Netlifyでは、公開したFastAPIのURLへ置き換えます。
VITE_API_URL=http://localhost:8000

# デモモード。true の間はログイン画面を出さず、全機能を管理者として使えます（提出用の初期値）。
# クライアントが本番でアカウント管理を有効化する時に false へ変更します（バックエンドの DEMO_MODE と必ず揃える）。
VITE_DEMO_MODE=true
